# Sorting Visualizer

Interactive visualizer for bubble, insertion, selection, merge, quick, and heap sort.

Open `index.html` locally or serve the folder:

```bash
python3 -m http.server 8000
```

Then visit http://localhost:8000.
