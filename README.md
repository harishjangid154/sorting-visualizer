# Algorithm Visualizer

Interactive visualizer for sorting, searching, array, and math algorithms. Open **Full catalog** in the sidebar for the complete list, including graph, tree, DP, and backtracking ideas that need a different canvas.

```bash
python3 -m http.server 8000
```

Then visit http://localhost:8000.
